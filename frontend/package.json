{
  "name": "car-world",
  "version": "1.0.0",
  "description": "Car World Website",
  "scripts": {
    "dev": "live-server --host=127.0.0.1 --port=8080 --open=./index.html",
    "start": "live-server --host=127.0.0.1 --port=8080 --open=./index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}